import {createMuiTheme} from '@material-ui/core/styles'

const themes = createMuiTheme({
  palette: {
    primary: {
      main:"#764abc"
    }
  },
  background: {
    default: "#fff"
  }
})

export default themes;